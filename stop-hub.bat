@echo off
setlocal

echo stop-hub.bat no longer stops the Vite dev server automatically.
echo Close the dev server terminal window instead.
echo.
echo To stop Docker-based app services, run stop-services.bat
pause
