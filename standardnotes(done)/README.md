# Standard Notes Local Stack

This folder now contains a local Docker setup for:

- Standard Notes sync server on `http://localhost:3004`
- Standard Notes web app on `http://localhost:3007`

Use the web app URL in the portal, then point it at the local sync server if the sign-in flow asks for a custom server.
